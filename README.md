# omarchy-sound-saga
> [!CAUTION]
> Current state: WIP

## Usage
  + install dependencies
  <!-- + copy files from original solution repo -->
  + run archlinux-saga.sh as sudo and follow the instructions
  + optionally add this to cmdline in limine.conf
  > snd_intel_dspcfg.dsp_driver=3
  + reboot
  + run postfix.sh and follow the instructions
